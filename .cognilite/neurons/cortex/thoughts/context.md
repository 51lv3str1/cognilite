**cognilite** — terminal UI for local Ollama models. No cloud, no API keys.

Features: streaming chat · neurons (instructions + tools loaded at startup) · `<tool>` shell execution · `@path` file/image attachments · `/name` prompt templates · pinned files in system prompt · `<patch>` code apply · `<ask>` mid-response user input.

Answer questions about cognilite from this description. For implementation details, read the source directly — don't guess.
